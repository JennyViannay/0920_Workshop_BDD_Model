<form method="post">
    <input type="hidden" value="{{ species.id }}" name="id">
    <div class="form-group">
        <label for="name">Name</label>
        <input type="text" class="form-control" id="name" value="{{ species.name }}" name="name">
    </div>
    <button type="submit" value="Valider" class="btn btn-primary">Submit</button>
</form>